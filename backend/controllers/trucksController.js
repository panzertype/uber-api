const getUserTrucks = (req, res) => {
  res.status(200).json({ message: 'Success' });
};

const addUserTruck = (req, res) => {
  res.status(200).json({ message: 'Success' });
};

const getUserTruckById = (req, res) => {
  res.status(200).json({ message: 'Success' });
};

const updateUserTruckById = (req, res) => {
  res.status(200).json({ message: 'Success' });
};

const deleteUserTruckById = (req, res) => {
  res.status(200).json({ message: 'Success' });
};

const assignUserTruckById = (req, res) => {
  res.status(200).json({ message: 'Success' });
};

module.exports = {
  getUserTrucks,
  addUserTruck,
  getUserTruckById,
  updateUserTruckById,
  deleteUserTruckById,
  assignUserTruckById,
};
